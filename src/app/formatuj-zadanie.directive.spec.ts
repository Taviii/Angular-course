import { FormatujZadanieDirective } from './formatuj-zadanie.directive';

describe('FormatujZadanieDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatujZadanieDirective();
    expect(directive).toBeTruthy();
  });
});
